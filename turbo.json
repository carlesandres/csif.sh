{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "validate": {
      "outputs": []
    },
    "render:docs": {
      "outputs": ["website/docs/registry/**"]
    },
    "render:build": {
      "outputs": ["build/registry-md/**"]
    },
    "start": {
      "cache": false,
      "persistent": true
    }
  }
}
